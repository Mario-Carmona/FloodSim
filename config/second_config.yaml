scenario:
  name: second_data_1

logging:
  level: info
  async: true
  silent: false
  file: simulation.log

input:
  type: file
  path: ../../../data/second_data

output:
  snapshot:
    every_n_steps: 180
  outputs:
    - type: image

state_updater:
  type: tensorflow
  model_path: ../../../models

simulation:
  time_step: 10.0
  total_time: 18000.0